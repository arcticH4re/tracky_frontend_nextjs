// next imports
import { NextPage } from "next";

// custom component
import DetailedConsumptionLayout from "../../../components/ui/layout/DetailedConsumptionLayout";

const WaterPage: NextPage = () => {
  return <DetailedConsumptionLayout />;
};

export default WaterPage;
